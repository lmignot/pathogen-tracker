<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@color/colorPrimary"
  android:gravity="bottom">

  <TextView
    android:id="@+id/questionTextPrimary"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:textColor="@color/white"
    android:textSize="24sp"
    android:text="@string/what_is_primary_activity"
    android:layout_marginBottom="164dp"
    android:layout_marginStart="24dp"
    android:layout_marginEnd="48dp"
    android:layout_alignParentBottom="true"
    />

  <RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:layout_alignParentBottom="true"
    android:paddingEnd="24dp"
    android:paddingStart="24dp"
    android:background="@color/white">

    <Button
      android:id="@+id/primaryActivityVector"
      android:layout_width="wrap_content"
      android:layout_height="match_parent"
      android:text="@string/mosquito"
      android:background="@color/white"
      android:textColor="@color/colorVectorAccent"
      android:layout_alignParentEnd="true"/>

    <Button
      android:id="@+id/primaryActivityPatient"
      android:layout_width="wrap_content"
      android:layout_height="match_parent"
      android:text="@string/patient"
      android:background="@color/white"
      android:textColor="@color/colorAccent"
      android:layout_alignParentStart="true"/>

  </RelativeLayout>

</RelativeLayout>
